{
    "version": "1.0.0",
    "configurations": [
        {
            "name": "Launch CTA page",
            "type": "chrome",
            "request": "launch",
            "file": "${workspaceFolder}/dist/index.html"
        },
        {
            "name": "Build",
            "type": "node-terminal",
            "request": "launch",
            "command": "npm run build",
            "cwd": "${workspaceFolder}"
        }
    ]
}
